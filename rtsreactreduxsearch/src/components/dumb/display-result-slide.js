import React, { Component } from "react";

export const DisplaySlide = props => (

        <div className='results-slide'>
        <span>{props.title}</span>
        </div>

);

export default DisplaySlide;